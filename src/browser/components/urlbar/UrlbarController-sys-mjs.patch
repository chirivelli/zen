diff --git a/browser/components/urlbar/UrlbarController.sys.mjs b/browser/components/urlbar/UrlbarController.sys.mjs
index 1111111111111111111111111111111111111111..2222222222222222222222222222222222222222 100644
--- a/browser/components/urlbar/UrlbarController.sys.mjs
+++ b/browser/components/urlbar/UrlbarController.sys.mjs
@@ -314,7 +314,6 @@
     const isMac = AppConstants.platform == "macosx";
     // Handle readline/emacs-style navigation bindings on Mac.
     if (
-      isMac &&
       this.view.isOpen &&
       event.ctrlKey &&
       (event.key == "n" || event.key == "p")
@@ -391,6 +390,15 @@
         event.preventDefault();
         break;
       case KeyEvent.DOM_VK_TAB: {
+        // Zen: Tab on a matching site suggestion enters that site's search mode
+        // (no "@" required). Falls through to normal Tab behaviour otherwise.
+        if (
+          executeAction &&
+          !(event.ctrlKey || event.altKey || event.shiftKey) &&
+          this.browserWindow.gZenUIManager?.maybeEnterSiteSearchMode?.(event)
+        ) {
+          break;
+        }
         if (!this.view.visibleRowCount) {
           // Leave it to the default behaviour if there are not results.
           break;
@@ -503,6 +511,8 @@
             });
           }
           event.preventDefault();
+        } else if (!this.input.value && !(event.ctrlKey || event.altKey || event.shiftKey)) {
+          this.browserWindow.gZenUIManager.enableCommandsMode(event);
         }
         break;
       }
